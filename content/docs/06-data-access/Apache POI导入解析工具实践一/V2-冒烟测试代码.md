---
title: "V2 冒烟测试代码 —— ExcelImportParserV2SmokeTest 完整归档"
description: "ExcelImportParserV2 V2 冒烟测试完整代码归档（465 行，13 个测试用例），覆盖 V1 全部场景（基本解析/空行跳过/单元格补齐/表头校验/关闭校验/自定义起始行/自定义表头行/异常包装）+ V2 新增场景（RowAccessor 多类型取值/空值返回 null/按索引取值/getRaw 原始值/越界安全）。"
weight: 5
---

# V2 冒烟测试代码

> **版本**：V2（基于 V1 的架构优化）  
> **文件**：ExcelImportParserV2SmokeTest.java（465 行，13 个测试用例）  
> **依赖**：JUnit 4、Apache POI（HSSFWorkbook）、BizException、SysRetCodeConstants

---

## 完整代码

```java
package com.demo;

import org.apache.poi.hssf.usermodel.HSSFWorkbook;
import org.apache.poi.ss.usermodel.Row;
import org.apache.poi.ss.usermodel.Sheet;
import org.apache.poi.ss.usermodel.Workbook;
import org.junit.Test;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.io.InputStream;
import java.math.BigDecimal;
import java.util.Arrays;
import java.util.List;
import java.util.Map;

import static org.junit.Assert.*;

/**
 * ExcelImportParserV2 冒烟测试
 * 覆盖 V1 全部场景 + RowAccessor 多类型取值
 */
public class ExcelImportParserV2SmokeTest {

    // ==================== 测试用 VO ====================

    static class TestVO {
        private String name;
        private String age;
        private String city;

        String getName() { return name; }
        void setName(String name) { this.name = name; }
        String getAge() { return age; }
        void setAge(String age) { this.age = age; }
        String getCity() { return city; }
        void setCity(String city) { this.city = city; }
    }

    static class NumericVO {
        private String name;
        private Integer quantity;
        private Long amount;
        private BigDecimal price;

        String getName() { return name; }
        void setName(String name) { this.name = name; }
        Integer getQuantity() { return quantity; }
        void setQuantity(Integer quantity) { this.quantity = quantity; }
        Long getAmount() { return amount; }
        void setAmount(Long amount) { this.amount = amount; }
        BigDecimal getPrice() { return price; }
        void setPrice(BigDecimal price) { this.price = price; }
    }

    // ==================== A1: 基本解析（RowAccessor.getString） ====================

    @Test
    public void testBasicParse_WithRowAccessor() throws Exception {
        InputStream is = createXls(
                new String[]{"姓名", "年龄", "城市"},
                new String[][]{
                        {"张三", "25", "北京"},
                        {"李四", "30", "上海"}
                });

        List<TestVO> result = ExcelImportParserV2.<TestVO>builder()
                .inputStream(is)
                .expectedTitles(Arrays.asList("姓名", "年龄", "城市"))
                .mapper((rowIndex, row) -> {
                    TestVO vo = new TestVO();
                    vo.setName(row.getString("姓名"));
                    vo.setAge(row.getString("年龄"));
                    vo.setCity(row.getString("城市"));
                    return vo;
                })
                .parse();

        assertEquals(2, result.size());
        assertEquals("张三", result.get(0).getName());
        assertEquals("25", result.get(0).getAge());
        assertEquals("北京", result.get(0).getCity());
        assertEquals("李四", result.get(1).getName());
    }

    // ==================== A2: 空行跳过 ====================

    @Test
    public void testEmptyRowSkipped() throws Exception {
        Workbook wb = new HSSFWorkbook();
        Sheet sheet = wb.createSheet();
        Row h = sheet.createRow(0);
        h.createCell(0).setCellValue("姓名");
        h.createCell(1).setCellValue("年龄");
        Row r1 = sheet.createRow(1);
        r1.createCell(0).setCellValue("张三");
        r1.createCell(1).setCellValue("25");
        sheet.createRow(2); // 空行
        Row r3 = sheet.createRow(3);
        r3.createCell(0).setCellValue("李四");
        r3.createCell(1).setCellValue("30");

        InputStream is = toInputStream(wb);
        wb.close();

        List<TestVO> result = ExcelImportParserV2.<TestVO>builder()
                .inputStream(is)
                .expectedTitles(Arrays.asList("姓名", "年龄"))
                .mapper((rowIndex, row) -> {
                    TestVO vo = new TestVO();
                    vo.setName(row.getString("姓名"));
                    vo.setAge(row.getString("年龄"));
                    return vo;
                })
                .parse();

        assertEquals("空行应被跳过", 2, result.size());
        assertEquals("张三", result.get(0).getName());
        assertEquals("李四", result.get(1).getName());
    }

    // ==================== A3: 单元格补齐 ====================

    @Test
    public void testCellPadding() throws Exception {
        Workbook wb = new HSSFWorkbook();
        Sheet sheet = wb.createSheet();
        Row h = sheet.createRow(0);
        h.createCell(0).setCellValue("姓名");
        h.createCell(1).setCellValue("年龄");
        h.createCell(2).setCellValue("城市");
        Row r1 = sheet.createRow(1);
        r1.createCell(0).setCellValue("张三");
        // 年龄和城市列缺失

        InputStream is = toInputStream(wb);
        wb.close();

        List<TestVO> result = ExcelImportParserV2.<TestVO>builder()
                .inputStream(is)
                .expectedTitles(Arrays.asList("姓名", "年龄", "城市"))
                .mapper((rowIndex, row) -> {
                    TestVO vo = new TestVO();
                    vo.setName(row.getString("姓名"));
                    vo.setAge(row.getString("年龄"));
                    vo.setCity(row.getString("城市"));
                    return vo;
                })
                .parse();

        assertEquals(1, result.size());
        assertEquals("张三", result.get(0).getName());
        assertEquals("缺失列应补齐为空串", "", result.get(0).getAge());
        assertEquals("缺失列应补齐为空串", "", result.get(0).getCity());
    }

    // ==================== A4: 表头不匹配 ====================

    @Test(expected = BizException.class)
    public void testHeaderMismatch_ThrowsBizException() throws Exception {
        InputStream is = createXls(
                new String[]{"姓名", "年龄"},
                new String[][]{{"张三", "25"}});

        ExcelImportParserV2.<TestVO>builder()
                .inputStream(is)
                .expectedTitles(Arrays.asList("名字", "年纪"))
                .mapper((rowIndex, row) -> new TestVO())
                .parse();
    }

    // ==================== A5: 关闭表头校验 ====================

    @Test
    public void testSkipHeaderValidation() throws Exception {
        InputStream is = createXls(
                new String[]{"名字", "年纪"},
                new String[][]{{"张三", "25"}});

        List<TestVO> result = ExcelImportParserV2.<TestVO>builder()
                .inputStream(is)
                .expectedTitles(Arrays.asList("姓名", "年龄"))
                .validateHeader(false)
                .mapper((rowIndex, row) -> {
                    TestVO vo = new TestVO();
                    vo.setName(row.getStringAt(0));
                    vo.setAge(row.getStringAt(1));
                    return vo;
                })
                .parse();

        assertEquals(1, result.size());
        assertEquals("张三", result.get(0).getName());
    }

    // ==================== A6: 自定义数据起始行 ====================

    @Test
    public void testCustomDataStartRow() throws Exception {
        Workbook wb = new HSSFWorkbook();
        Sheet sheet = wb.createSheet();
        Row h = sheet.createRow(0);
        h.createCell(0).setCellValue("姓名");
        h.createCell(1).setCellValue("年龄");
        Row desc = sheet.createRow(1);
        desc.createCell(0).setCellValue("请填写信息");
        desc.createCell(1).setCellValue("请勿留空");
        Row r = sheet.createRow(2);
        r.createCell(0).setCellValue("张三");
        r.createCell(1).setCellValue("25");

        InputStream is = toInputStream(wb);
        wb.close();

        List<TestVO> result = ExcelImportParserV2.<TestVO>builder()
                .inputStream(is)
                .expectedTitles(Arrays.asList("姓名", "年龄"))
                .dataStartRow(2)
                .mapper((rowIndex, row) -> {
                    TestVO vo = new TestVO();
                    vo.setName(row.getString("姓名"));
                    vo.setAge(row.getString("年龄"));
                    return vo;
                })
                .parse();

        assertEquals(1, result.size());
        assertEquals("张三", result.get(0).getName());
    }

    // ==================== A7: 自定义表头行 ====================

    @Test
    public void testCustomHeaderRowIndex() throws Exception {
        Workbook wb = new HSSFWorkbook();
        Sheet sheet = wb.createSheet();
        Row title = sheet.createRow(0);
        title.createCell(0).setCellValue("员工信息报表");
        Row h = sheet.createRow(1);
        h.createCell(0).setCellValue("姓名");
        h.createCell(1).setCellValue("年龄");
        Row r = sheet.createRow(2);
        r.createCell(0).setCellValue("张三");
        r.createCell(1).setCellValue("25");

        InputStream is = toInputStream(wb);
        wb.close();

        List<TestVO> result = ExcelImportParserV2.<TestVO>builder()
                .inputStream(is)
                .expectedTitles(Arrays.asList("姓名", "年龄"))
                .headerRowIndex(1)
                .dataStartRow(2)
                .mapper((rowIndex, row) -> {
                    TestVO vo = new TestVO();
                    vo.setName(row.getString("姓名"));
                    vo.setAge(row.getString("年龄"));
                    return vo;
                })
                .parse();

        assertEquals(1, result.size());
        assertEquals("张三", result.get(0).getName());
        assertEquals("25", result.get(0).getAge());
    }

    // ==================== A8: 异常包装 ====================

    @Test(expected = BizException.class)
    public void testExceptionWrapped() {
        InputStream brokenStream = new InputStream() {
            @Override
            public int read() throws IOException {
                throw new IOException("模拟 IO 异常");
            }
        };

        ExcelImportParserV2.<TestVO>builder()
                .inputStream(brokenStream)
                .expectedTitles(Arrays.asList("姓名"))
                .mapper((rowIndex, row) -> new TestVO())
                .parse();
    }

    // ==================== B1: RowAccessor 多类型取值 ====================

    @Test
    public void testRowAccessor_TypedGetters() throws Exception {
        InputStream is = createXls(
                new String[]{"名称", "数量", "金额", "单价"},
                new String[][]{{"零件A", "100", "50000", "500.50"}});

        List<NumericVO> result = ExcelImportParserV2.<NumericVO>builder()
                .inputStream(is)
                .expectedTitles(Arrays.asList("名称", "数量", "金额", "单价"))
                .mapper((rowIndex, row) -> {
                    NumericVO vo = new NumericVO();
                    vo.setName(row.getString("名称"));
                    vo.setQuantity(row.getInteger("数量"));
                    vo.setAmount(row.getLong("金额"));
                    vo.setPrice(row.getDecimal("单价"));
                    return vo;
                })
                .parse();

        assertEquals(1, result.size());
        assertEquals("零件A", result.get(0).getName());
        assertEquals("getInteger 应正确转换", Integer.valueOf(100), result.get(0).getQuantity());
        assertEquals("getLong 应正确转换", Long.valueOf(50000), result.get(0).getAmount());
        assertEquals("getDecimal 应正确转换", new BigDecimal("500.50"), result.get(0).getPrice());
    }

    // ==================== B2: RowAccessor 空值返回 ====================

    @Test
    public void testRowAccessor_EmptyReturnsNull() throws Exception {
        Workbook wb = new HSSFWorkbook();
        Sheet sheet = wb.createSheet();
        Row h = sheet.createRow(0);
        h.createCell(0).setCellValue("名称");
        h.createCell(1).setCellValue("数量");
        Row r = sheet.createRow(1);
        r.createCell(0).setCellValue("零件A");
        // 数量列缺失

        InputStream is = toInputStream(wb);
        wb.close();

        List<NumericVO> result = ExcelImportParserV2.<NumericVO>builder()
                .inputStream(is)
                .expectedTitles(Arrays.asList("名称", "数量"))
                .mapper((rowIndex, row) -> {
                    NumericVO vo = new NumericVO();
                    vo.setName(row.getString("名称"));
                    vo.setQuantity(row.getInteger("数量"));     // 空 → null
                    vo.setAmount(row.getLong("不存在的列"));     // 不存在 → null
                    vo.setPrice(row.getDecimal("也不存在"));    // 不存在 → null
                    return vo;
                })
                .parse();

        assertEquals(1, result.size());
        assertEquals("零件A", result.get(0).getName());
        assertNull("空单元格 getInteger 应返回 null", result.get(0).getQuantity());
        assertNull("不存在的列 getLong 应返回 null", result.get(0).getAmount());
        assertNull("不存在的列 getDecimal 应返回 null", result.get(0).getPrice());
    }

    // ==================== B3: RowAccessor 按索引取值 ====================

    @Test
    public void testRowAccessor_GetByIndex() throws Exception {
        InputStream is = createXls(
                new String[]{"姓名", "年龄", "城市"},
                new String[][]{{"张三", "25", "北京"}});

        List<TestVO> result = ExcelImportParserV2.<TestVO>builder()
                .inputStream(is)
                .expectedTitles(Arrays.asList("姓名", "年龄", "城市"))
                .mapper((rowIndex, row) -> {
                    TestVO vo = new TestVO();
                    vo.setName(row.getStringAt(0));
                    vo.setAge(row.getStringAt(1));
                    vo.setCity(row.getStringAt(2));
                    return vo;
                })
                .parse();

        assertEquals(1, result.size());
        assertEquals("张三", result.get(0).getName());
        assertEquals("25", result.get(0).getAge());
        assertEquals("北京", result.get(0).getCity());
    }

    // ==================== B4: RowAccessor getRaw 获取原始值 ====================

    @Test
    public void testRowAccessor_GetRaw() throws Exception {
        Workbook wb = new HSSFWorkbook();
        Sheet sheet = wb.createSheet();
        Row h = sheet.createRow(0);
        h.createCell(0).setCellValue("名称");
        h.createCell(1).setCellValue("数量");
        Row r = sheet.createRow(1);
        r.createCell(0).setCellValue("零件A");
        r.createCell(1).setCellValue(42.0); // 数值单元格，SAX 读取为 Double

        InputStream is = toInputStream(wb);
        wb.close();

        // 用 getRaw 验证原始值类型
        ExcelImportParserV2.<Object>builder()
                .inputStream(is)
                .expectedTitles(Arrays.asList("名称", "数量"))
                .mapper((rowIndex, row) -> {
                    Object nameRaw = row.getRaw("名称");
                    assertTrue("字符串单元格应为 String", nameRaw instanceof String);
                    assertEquals("零件A", nameRaw);

                    Object qtyRaw = row.getRaw("数量");
                    // SAX 模式下数值单元格可能是 Double 或 String
                    assertNotNull("数值单元格不应为 null", qtyRaw);
                    return qtyRaw;
                })
                .parse();
    }

    // ==================== B5: RowAccessor 越界安全 ====================

    @Test
    public void testRowAccessor_OutOfBoundsSafe() throws Exception {
        InputStream is = createXls(
                new String[]{"姓名"},
                new String[][]{{"张三"}});

        List<TestVO> result = ExcelImportParserV2.<TestVO>builder()
                .inputStream(is)
                .expectedTitles(Arrays.asList("姓名"))
                .mapper((rowIndex, row) -> {
                    TestVO vo = new TestVO();
                    vo.setName(row.getString("姓名"));
                    // 访问不存在的列和越界索引
                    vo.setAge(row.getString("不存在的列"));   // → ""
                    vo.setCity(row.getStringAt(99));           // → ""
                    assertNull(row.getInteger("不存在的列"));  // → null
                    assertNull(row.getLongAt(99));              // → null
                    assertNull(row.getDecimalAt(-1));           // → null
                    assertNull(row.getRawAt(99));               // → null
                    return vo;
                })
                .parse();

        assertEquals(1, result.size());
        assertEquals("张三", result.get(0).getName());
        assertEquals("", result.get(0).getAge());
        assertEquals("", result.get(0).getCity());
    }

    // ==================== 辅助方法 ====================

    private InputStream createXls(String[] titles, String[][] data) throws Exception {
        Workbook wb = new HSSFWorkbook();
        Sheet sheet = wb.createSheet();
        Row headerRow = sheet.createRow(0);
        for (int i = 0; i < titles.length; i++) {
            headerRow.createCell(i).setCellValue(titles[i]);
        }
        for (int r = 0; r < data.length; r++) {
            Row row = sheet.createRow(r + 1);
            for (int c = 0; c < data[r].length; c++) {
                row.createCell(c).setCellValue(data[r][c]);
            }
        }
        return toInputStream(wb);
    }

    private InputStream toInputStream(Workbook wb) throws Exception {
        ByteArrayOutputStream bos = new ByteArrayOutputStream();
        wb.write(bos);
        wb.close();
        return new ByteArrayInputStream(bos.toByteArray());
    }
}
```

---

## 覆盖矩阵

### A 组：V1 继承场景（8 个）

| 编号 | 测试用例 | 验证点 |
|------|----------|--------|
| A1 | testBasicParse_WithRowAccessor | 基本解析 + RowAccessor.getString |
| A2 | testEmptyRowSkipped | 空行自动跳过 |
| A3 | testCellPadding | 列数不足时补齐为空串 |
| A4 | testHeaderMismatch_ThrowsBizException | 表头不匹配抛 BizException |
| A5 | testSkipHeaderValidation | validateHeader(false) 关闭校验 + getStringAt |
| A6 | testCustomDataStartRow | dataStartRow 跳过说明行 |
| A7 | testCustomHeaderRowIndex | 自定义表头行 + dataStartRow |
| A8 | testExceptionWrapped | IOException 包装为 BizException |

### B 组：V2 新增场景（5 个）

| 编号 | 测试用例 | 验证点 |
|------|----------|--------|
| B1 | testRowAccessor_TypedGetters | getInteger / getLong / getDecimal 多类型取值 |
| B2 | testRowAccessor_EmptyReturnsNull | 空单元格/不存在列 → getInteger/getLong/getDecimal 返回 null |
| B3 | testRowAccessor_GetByIndex | getStringAt 按索引取值 |
| B4 | testRowAccessor_GetRaw | getRaw 获取原始 Object 值 |
| B5 | testRowAccessor_OutOfBoundsSafe | 不存在列名/越界索引 → 各方法安全返回 "" 或 null |
